/target
*.jar

# ── Spring profile properties with real secrets (NEVER commit these) ──────────
src/main/resources/application-dev.properties
src/main/resources/application-prod.properties
# Keep: application.properties (base, no secrets)
# Keep: application-*.properties.template (safe placeholders)

.settings/
.classpath
.project
.idea/